import React from 'react';
import ServiceInteractive from './ServiceInteractive.client';

export default function ServicesSection() {
    return (
        <section className="py-16 bg-[#0a0a0a] px-6 lg:px-12">
            <ServiceInteractive />
        </section>
    );
}
